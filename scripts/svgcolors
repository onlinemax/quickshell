#!/bin/env bash

file=$1
# stroke or fill
option=$2
#color
color=$3

if [ -z "$file" ] || [ -z "$option" ] || [ -z "$color" ]; then
	echo "Missing options"
	exit 1
fi

sed -E -i "s/$option=\"#?[a-zA-Z0-9]+\"/$option=\"$color\"/" $file

